from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/deploy', methods=['POST', 'GET'])
def deploy():
return jsonify({"message": "Deploy command received"}), 200

if __name__ == '__main__':
app.run()
